# neo-brick-crush app

이 디렉터리는 `neo-brick-crush`의 실제 앱 루트다.

## Commands

```bash
npm install
npm run dev
```

## Notes

- `Phaser = runtime`
- `XState = session/progression orchestration`
- `Zustand = selector-based lightweight UI state`
- `React = presentation`
- 외부 연동은 `platform/` 경계에서만 수행한다.
